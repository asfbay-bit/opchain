# Feature spec — Demo Workbench Search & Filter

Scoped enhancement to the `/demo` surface. Generated by **oc-app-architect**
(`/oc-app`) Phase 2. Lives separately from the project master spec
(`specs/spec/`).

| Doc | Contents |
|---|---|
| [`00-overview.md`](./00-overview.md) | Problem, personas, anti-goals, success metrics, scope. |
| [`01-tech-stack.md`](./01-tech-stack.md) | Inherited stack (validated); the hand-rolled-index search-engine decision + the MiniSearch tripwire; feature flag. |
| [`02-architecture.md`](./02-architecture.md) | Data-model changes (`phase`, kind normalization, step anchors), build-time index + schema, client engine, UI surfaces, URL/deep-link contract, security, files touched. |
| [`06-testing.md`](./06-testing.md) | Unit + e2e + a11y + budget plan; definition of done. |
| [`08-analytics.md`](./08-analytics.md) | Consent-gated PostHog events; privacy rules (no raw query text). |

**Not applicable** (intentionally omitted): `03-security-auth` (no auth; the
client-side note lives in `02 §6`), `04-integrations`, `05-monetization`,
`07-devops` (unchanged — manual `wrangler deploy`), `09-cost-estimate` (no new
infra), `10-documentation-plan` (covered by tests + this spec).

## Discovery decisions (locked)

Deep full-text · facets: skill/role/artifact-kind/pipeline-phase ·
jump-to-exchange + shareable URLs · explicit `phase` tags on beats (all 12
scenarios) · full mobile parity · build-time index + client filter, zero new
runtime deps.

## Status

**Implemented** on `claude/relaxed-cannon-ut762y` (spec ✓, design ✓, build ✓).
Lib in `site/src/lib/demo-search/`; UI wired into both workbenches; flag
`site.feature.demo-search`. 32 Vitest unit tests + index-size budget guard
(full suite green), `astro check` clean, 8 Playwright e2e specs (run in CI).
Next: PR + CI, then Phase 7 launch (audit → manual deploy per CLAUDE.md).
