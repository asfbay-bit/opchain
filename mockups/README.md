# mockups/

Static HTML mockups for the `kind:pack` stack-pack UI (desktop + mobile),
used during design review. Not part of the build, not linked from the
live site — reference-only. If the pack UI ships, the real implementation
lands in `site/src/`.
