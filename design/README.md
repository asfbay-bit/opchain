# design/

Working design artifacts for in-flight or recently-shipped architecture
diagram work — proposals, functional variants, and punchlists that back a
specific release (currently v1.6/v1.7 architecture diagrams). Not part of
the build, not linked from the live site.

Distinct from `design-previews/`, which holds older, closed-out design
rounds kept for historical reference rather than active work.
