# opchain 1.7 — "Seams & Signals" — Release Plan

> **Status:** PLAN (design briefs only — no skills built yet).
> **Produced by:** `oc-app-architect` planning phases (discovery → spec → roadmap),
> treating "the app" as three new opchain skills.
> **Date:** 2026-06-26 · **Branch:** `claude/signals-modularization-planning-7mvv3j`
> **Revised 2026-06-26** after an adversarial verification pass (5 independent reviewers
> fact-checking every boundary + ecosystem claim against the live repo). Corrections are
> folded in; the "Verification corrections" appendix lists what changed and why.

Three new skills, taking the catalog from **24 → 27**, lockstep-bumped to **1.7.0**.

| # | Skill | Command | Family | One-line mission |
|---|---|---|---|---|
| 1 | **oc-signal-forge** | `/oc-signal` | `-forge` (Designer/Builder/Evaluator) | Turn a *question* into a trustworthy metric: instrument it, harvest it, and **prove the signal answers the question** before wiring it to a surface. |
| 2 | **oc-modularize-ops** | `/oc-modularize` | `-ops` (gated phases) | Decompose a monolith into modules with **provably zero functionality or data loss** — capturing golden fixtures from *real* data — and refuse when modularization isn't warranted. |
| 3 | **oc-fleet-ops** | `/oc-fleet` | `-ops` (gated phases) | Provision, deploy, and operate **one-or-more containers across arbitrary self-managed environments** (k8s/Nomad/Compose, IaC, on-prem VMs, GCE) — the territory `oc-deploy-ops` routes elsewhere today. |

**The 1.7 throughline (two new chains):**

```
oc-signal-forge ──► oc-dash-forge          (build trustworthy signals → render them)
                └─► oc-monitoring-ops       (enforce signal-freshness SLAs in prod)

oc-modularize-ops ──► oc-migration-ops ──► oc-fleet-ops
   (decide + characterize + plan + verify)   (Structural-type   (deploy the pieces)
                                               cutover/execute)
                  └────────────────────────► oc-fleet-ops
                          (deploy the modules directly)
```

Release theme name candidate: **"Seams & Signals"** (1.5 was "Build the AI app",
1.6 "The instrumented pipeline").

---

## Why these are three *new* skills (not extensions)

Each new skill sits next to an existing one whose edge it touches. The boundary, in
one line each:

- **oc-signal-forge vs the instrumentation skills.** `oc-telemetry-ops` meters the
  *opchain pipeline's own* usage (opt-in, local SQLite). `oc-dash-forge` renders the
  *front end*. `oc-monitoring-ops` watches *prod uptime/errors*. None of them build the
  **product's analytics backend** — the instrumentation, the harvester, the transform,
  and the proof the number is right. That's the gap.
- **oc-modularize-ops vs oc-migration-ops.** migration-ops already has a *"Structural"*
  type, and it is **fully wired**: it auto-detects *"Split the monorepo" → Structural*
  and carries `files_moved` / `imports_updated` state. So the differentiator is **not**
  "migration-ops only does it lightly." The real boundary is the **golden-fixture
  equivalence oracle**: a *live monolith carrying real prod traffic that must be split
  with a provable no-loss guarantee* → modularize-ops; a *repo/package reorg where
  imports move but behaviour is git-diffable* → migration-ops Structural. modularize-ops
  then **hands the bulk code-move and live cutover back to migration-ops's
  Structural-type `/oc-migrate execute`** — it owns the decision, characterization,
  seam contracts, and equivalence proof, not a second code-mover.
- **oc-fleet-ops vs oc-deploy-ops.** deploy-ops is opinionated about *single-app managed
  PaaS*. Its Platform Matrix's *"What's NOT first-class"* table reads, verbatim:
  *"Bare-metal / VPS | Out of scope | oc-deploy-ops is opinionated about managed deploys;
  bare-metal needs **oc-migration-ops**, not oc-deploy-ops."* So that territory is
  **currently routed to the existing `oc-migration-ops`, not vacant.** 1.7's change is a
  **behavioural re-point**: S5 edits that row to send multi-node/self-managed/IaC deploys
  to `oc-fleet-ops`. The justification: migration-ops is a *transformation engine* (engine
  swaps, cutovers) with **no provision / topology / fleet-health surface** — it was only
  ever the bare-metal pointer by default. fleet-ops gives that territory a real home.

Keeping them separate preserves the ecosystem's single-responsibility skills and their
existing trigger surfaces, instead of bloating migration-ops and deploy-ops with blurry
second modes — *provided* each new skill earns its keep against the boundaries above.

---

# Skill 1 — `oc-signal-forge`

## Proposed frontmatter

```yaml
name: oc-signal-forge
displayName: OC · Signal Forge
version: 1.7.0
shortDesc: Question → trustworthy metric. Instrument it, harvest it, prove it answers the question, then wire it to a surface. Backend + middleware only.
phases: [build]            # Designer/Builder/Evaluator all run inside the build phase
                           # (VALID_PHASES = foundation|plan|build|ai-native — "design" is NOT valid)
triAgent: true             # Designer / Builder / Evaluator
tryable: true
commands:
  - /oc-signal
  - /oc-signal frame
  - /oc-signal design
  - /oc-signal harvest
  - /oc-signal build
  - /oc-signal verify
  - /oc-signal wire
  - /oc-signal catalog
  - /oc-signal status
description: >
  Analytics & signals backend harness with a Designer/Builder/Evaluator loop. Derives
  new metrics from the question they answer, builds the instrumentation + harvester +
  transform, and adversarially verifies the signal is correct AND answers the question
  before wiring it to a consumer. Use for /oc-signal, "new metric", "instrument this",
  "analytics backend", "data harvesting", "is this metric right", "wire up a signal",
  "derive a KPI". Hands rendered output to oc-dash-forge. NOT pipeline telemetry
  (oc-telemetry-ops), NOT dashboards (oc-dash-forge), NOT prod uptime (oc-monitoring-ops).
  Trigger liberally on product-analytics / metric-engineering work.
governance:
  breaking_change_policy: skills/CHANGELOG.md
  last_reviewed: 2026-06-26
  owner: opchain
```

## Mission

Owns the **backend of analytics**: instrumentation design, the harvester / ingestion
layer, the transform / middleware, and — the part most analytics tooling skips —
**validation that the signal answers the question correctly**, before it's exposed.
The dashboarding front end already exists (`oc-dash-forge`); this is the skill that
*feeds* it a number you can trust.

## Operating model — Designer → Builder → Evaluator

The `-forge` tri-agent loop is the right shape because the hard part isn't laying pipe,
it's proving the number is right. The **Evaluator is adversarial about correctness.**

```
/oc-signal frame      The QUESTION + decision the metric serves (the anchor)
        │
        ▼
┌──────────────┐  signal spec   ┌──────────────┐
│  DESIGNER    │───────────────►│  BUILDER     │  instrument + harvest + transform + store
│ define the   │                │ implement    │
│ metric +     │◄──negotiate───►│ the pipeline │
│ instrument-  │                └──────┬───────┘
│ ation spec   │                       │ signal (live)
└──────────────┘                       ▼
                              ┌──────────────────┐
                              │  EVALUATOR       │  "does it answer the question,
                              │ skeptical:       │   and is the answer RIGHT?"
                              │ correctness +    │
                              │ answers-the-Q +  │  PASS → /oc-signal wire
                              │ robustness +     │  FAIL → back to Builder
                              │ wired?           │
                              └──────────────────┘
```

### Phase 0 — Frame (`/oc-signal frame`)
State, in one sentence, **the decision this metric informs** and the question it
answers. This is the anchor the Evaluator grades everything against. Without it you get
a number nobody can act on.

### Phase 1 — Designer (`/oc-signal design`)
Produce the **signal spec**:
- The question it answers + the decision it drives.
- Metric definition: formula, numerator/denominator, unit, **grain** (per-request /
  session / user / day), source of truth.
- Instrumentation schema: event names, properties, identity/keys, sampling, PII stance.
- "What would make this *wrong*": double-counting, bot/replay traffic, timezone,
  identity stitching, late-arriving data, survivorship/selection bias.

### Phase 2 — Builder (`/oc-signal build`, `/oc-signal harvest`)
Implement the pipeline. The **harvester is an explicit abstraction** so the same skill
handles any ingestion shape:

| Harvester archetype | When to pick it | Example |
|---|---|---|
| In-process emit | you own the code path | analytics SDK call at the event site |
| Sidecar / worker container | heavy transform, out-of-band | a consumer container off a queue |
| CI / scheduled job | batch, periodic recompute | nightly cron recomputing a cohort metric |
| Endpoint / webhook pull | data lives in a 3rd-party API | pull Stripe/CRM, or receive a webhook |
| Log / trace scrape | you can't change the source | parse structured logs |

Plus the **middleware/transform** (clean, dedupe, enrich, aggregate; idempotency +
late-data handling), the **store** (events table / warehouse / time-series — defers the
platform choice to `oc-stack-forge`), and a **consumer stub** (the query/endpoint
`oc-dash-forge` or an app will read).

### Phase 3 — Evaluator (`/oc-signal verify`) — the signature gate
Skeptical pass, graded on four axes:

| Axis | What it catches |
|---|---|
| **Answers-the-question** | proxy-metric drift — e.g. "active users" built as raw page-loads when the question was about *engaged* users. The most common silent failure. |
| **Correctness** | reconcile against an independent ground truth (a hand-written SQL count, a known reference period, a back-of-envelope). Distribution sanity: no impossible values, expected cardinality, no silent zeros. |
| **Robustness** | double-count, bot/replay, timezone, identity-stitch, late-arriving, sampling-bias. |
| **Wired** | the signal actually reaches its consumer and the read contract is stable. (Freshness is *defined* here as `freshness_sla`, then **enforced in prod by `oc-monitoring-ops`** — see boundaries.) |

FAIL → Builder fixes → re-verify (same loop discipline as app-architect's
Generator/Evaluator).

### Phase 4 — Wire (`/oc-signal wire`)
Hand the *validated* signal to its consumer — chains to `oc-dash-forge` for rendering,
or `oc-api-dev` if the metric is exposed as a first-party endpoint.

## Signature artifact — the signal catalog (`/oc-signal catalog`)
A living registry (`signals/catalog.md` + `skill_state.signals[]`): every metric with
its **question**, definition, harvester type, source, consumer, freshness SLA, and
last-verified date. This is the antidote to metric sprawl — it makes "which *active
users* is this?" answerable.

## Boundaries — what oc-signal-forge does NOT own
| Concern | Owner |
|---|---|
| Pipeline usage metering (opt-in, local) | `oc-telemetry-ops` |
| Rendering the dashboard / charts | `oc-dash-forge` |
| Prod uptime / error / SLO observability | `oc-monitoring-ops` |
| **Alerting on signal staleness in prod** | `oc-monitoring-ops` (it has a first-class *Data-freshness* SLI + the v1.6 eval-drift template). signal-forge **defines** `freshness_sla`; monitoring-ops **enforces** it. |
| LLM spend attribution specifically | `oc-cost-ops` |
| Where the warehouse/store lives | `oc-stack-forge` (signal-forge consumes the decision) |

## Cross-skill wiring
| Reads from | Why |
|---|---|
| `oc-app-architect` (`08-analytics.md`) | the analytics *plan*; signal-forge *executes* it |
| `oc-stack-forge` | store / warehouse / time-series choice |
| `oc-api-dev` | if the metric is exposed via a first-party endpoint |

| Chains to | Why |
|---|---|
| `oc-dash-forge` | render the validated signal |
| `oc-monitoring-ops` | hand off each signal's `freshness_sla` so monitoring-ops enforces it via its Data-freshness SLI + AI-app eval-drift template (signal-forge owns the *contract*, monitoring-ops owns the *alert*) |
| `oc-api-dev` | publish the metric as an endpoint |

> **Resolves an existing seam:** `oc-app-architect`'s Phase-2 spec set already contains
> `08-analytics.md`. Today that doc is a plan with no executor. In 1.7, app-architect's
> Phase 2 chains to `oc-signal-forge` when the analytics doc warrants real instrumentation —
> the plan finally has a skill that builds it.

## PM-Tool MCP Integration (matches the ecosystem convention)
Like its neighbour `oc-dash-forge`, signal-forge posts to a linked ticket when one is in
context (e.g. `/oc-signal --ticket TICKET-1234`). It defers the runtime contract (tool
names, retry/backoff, idempotency markers, `pm_deferred_actions[]`) to
`oc-integrations-engineer/references/pm-mcp-protocol.md` and only shapes:
- **Signal spec posted** (Designer) → comment carrying the question + metric definition,
  marker `<!-- opchain:oc-signal-forge:signal-spec:<signal-id> -->`.
- **Evaluator verdict** (verify) → comment with the PASS/FAIL + the ground-truth
  reconciliation result, marker `<!-- opchain:oc-signal-forge:verify:<signal-id> -->`;
  a FAIL leaves the ticket open with the failing axis named.
- Records comment ids in `skill_state.pm.signal_comments[]`. No ticket in context → no
  PM write (operates as signals-only).

## Checkpoint
```jsonc
"progress_table": [
  { "id": "frame",        "label": "Frame the question",        "status": "not_started" },
  { "id": "design",       "label": "Signal spec (Designer)",    "status": "not_started" },
  { "id": "design-gate",  "label": "★ Spec approval",           "status": "not_started" },
  { "id": "harvest",      "label": "Harvester selection",       "status": "not_started" },
  { "id": "build",        "label": "Build pipeline (Builder)",  "status": "not_started" },
  { "id": "verify",       "label": "Evaluator gate",            "status": "not_started" },
  { "id": "verify-gate",  "label": "★ Correctness PASS",        "status": "not_started" },
  { "id": "wire",         "label": "Wire to consumer",          "status": "not_started" }
]
```

```jsonc
"skill_state": {
  "signals": [
    {
      "id": "weekly-active-teams",
      "question": "Are teams coming back week over week?",
      "definition": "distinct team_id with >=1 qualifying action / ISO week",
      "harvester_type": "scheduled-job",
      "store": "warehouse.fact_actions",
      "consumer": "oc-dash-forge:exec/retention",
      "freshness_sla": "24h",
      "status": "verified",
      "last_verified": "2026-07-xxTxx:xxZ",
      "ground_truth_check": "matched hand SQL ±0"
    }
  ]
}
```

## references/
- `signal-design.md` — metric-definition discipline, the proxy-metric traps, grain & identity.
- `harvester-patterns.md` — the 5 archetypes, idempotency, late-data, sampling.
- `signal-verification.md` — ground-truth reconciliation, distribution checks, the answers-the-question rubric.

## Principles
1. **A metric without a question is noise.** Frame first.
2. **The Evaluator's job is to disprove the number.** 5/10 is fine.
3. **Reconcile against an independent truth.** A pipeline that runs is not a pipeline that's right.
4. **The harvester is a choice, not a default.** Container vs CI vs pull vs scrape — pick to fit the source.
5. **Catalog every signal.** Definition ambiguity is the silent killer of analytics trust.

---

# Skill 2 — `oc-modularize-ops`

## Proposed frontmatter

```yaml
name: oc-modularize-ops
displayName: OC · Modularize Ops
version: 1.7.0
shortDesc: Decompose a monolith with provably zero functionality/data loss — golden fixtures from real data as the equivalence oracle. Willing to say "don't."
phases: [plan, build]      # plan = assess/characterize/plan; build = seam contracts + façade
                           # adapters + fixture-capture harness + equivalence verifier.
                           # The bulk module code-move is delegated to oc-migration-ops.
triAgent: false
tryable: true
commands:
  - /oc-modularize
  - /oc-modularize assess
  - /oc-modularize characterize
  - /oc-modularize plan
  - /oc-modularize extract
  - /oc-modularize verify
  - /oc-modularize diff
  - /oc-modularize status
  - /oc-modularize abandon
description: >
  Monolith decomposition operator. Decides whether modularization is even the right call
  (and is willing to say no), captures golden fixtures from REAL data for every boundary
  as an equivalence oracle, plans the seams + data ownership, and proves zero
  functionality/data loss by replaying the fixtures. Supports parallel-copy (duplicate →
  modularize the copy → swap when ready) and strangler-fig strategies. Hands the bulk
  code-move + live cutover to oc-migration-ops (Structural type) and per-module deployment
  to oc-fleet-ops. Use for /oc-modularize, "break up the monolith", "extract a service
  from a live system", "split this codebase safely", "golden fixtures", "no functionality
  loss". For a behaviour-git-diffable repo/package reorg, use oc-migration-ops Structural
  instead. Trigger when decomposing a LIVE monolith carrying real traffic.
governance:
  breaking_change_policy: skills/CHANGELOG.md
  last_reviewed: 2026-06-26
  owner: opchain
```

## Mission

Take a monolith — a codebase, a container, or a large deployment — and decompose it into
modules/services **without losing functionality or data**, and be willing to recommend
*not* doing it. The signature commitment: the split is **provably** behavior- and
data-preserving, verified against **real** input/output captured at every boundary.

### Boundary vs `oc-migration-ops` Structural (read this first)
migration-ops's Structural type already auto-detects *"Split the monorepo"* and owns
`files_moved` / `imports_updated`. The two must not both fire on the same request, so the
routing trigger is explicit:

| Signal | Routes to |
|---|---|
| Live monolith, real prod traffic, must prove no functionality/data loss with a real-data oracle | **oc-modularize-ops** |
| Repo/package reorg where imports move and behaviour is git-diffable (no live-traffic equivalence risk) | **oc-migration-ops** Structural |

modularize-ops **deconfliction note:** when a request matches both, modularize-ops runs
first (assess → characterize → plan → verify), then *emits a Structural migration plan*
that `oc-migration-ops /oc-migrate execute` runs for the actual code-move and cutover.
modularize-ops never silently competes with the Structural auto-detect — it owns the
*decision + oracle*, migration-ops owns the *move*.

## Operating model — gated ops phases

```
assess ──★should-we?──► characterize ──► plan ──★strategy?──► extract (×N) ──► verify ──► cutover handoff
 (is it      (CAN say     (golden          (seams +              (seam contract   (replay      (→ migration-ops
  worth it?)   "no")        fixtures from    data ownership +      + façade; bulk    fixtures →   Structural execute
                            REAL data)        strategy)            move delegated)   equivalence  for move+cutover,
                                                                                     + integrity)  → fleet-ops deploy)
```

### Phase 0 — Assess (`/oc-modularize assess`) — *can say "don't"*
Inventory the monolith (reuse `oc-reverse-spec` if present). Score modularization
**fitness** honestly:

| Justifies a split | Anti-signal (leave it a monolith) |
|---|---|
| independent scaling needs per area | premature — no real coupling pain yet |
| team-ownership boundaries forming | small team, one deploy cadence is fine |
| deploy-coupling / blast-radius pain | a split would just add a network tax + distributed-systems complexity |
| divergent runtime/lib needs | the "modules" are deeply chatty — latency would regress |

Output is a **recommendation that may be "don't modularize" + why**, or a candidate
module map with rationale. This is a real gate — the skill mirrors `oc-migration-ops`'s
honesty about *"break it into sequential migrations / don't do this all at once."*

### Phase 1 — Characterize (`/oc-modularize characterize`) — the signature move
For every boundary a split would cross — **endpoints, APIs, queue consumers, DB access
patterns, cross-module calls, cron jobs** — capture **golden fixtures from real data**:
- Record real `(input → output)` pairs at each boundary (request/response, message
  in/out, query → rows), sampled from prod or a **prod-faithful replica**.
- **From real data, not mock.** Mock data is explicitly *disallowed* as the equivalence
  oracle — the whole point is that the fixtures reflect what actually happens.
- **PII handling:** redact or synthesize-preserving-shape where needed, but keep the
  behavioral signal. (This phase loops `oc-security-auditor` — see Risks R1.)
- **Coverage gate:** every crossed boundary needs fixtures, **including error paths and
  edge cases**. **No fixture → no split** for that seam.

These fixtures become the **equivalence oracle** for Phase 4.

### Phase 2 — Plan seams (`/oc-modularize plan`) — *strategy gate*
Define module boundaries, the **seam contracts** between them, and **data ownership**
(which module owns which tables; shared data → split, duplicate, or a shared service).
Choose the strategy:

| Strategy | Shape |
|---|---|
| **Parallel-copy** *(preferred)* | duplicate the codebase, modularize the **copy**, keep the prod monolith serving, swap over only when the copy is verified equivalent. |
| **Strangler-fig / in-place** | extract one module at a time behind a façade, route incrementally, monolith shrinks over time. |

### Phase 3 — Extract (`/oc-modularize extract [module]`)
Produce, **one module at a time**: the module's **seam contract** and a **façade/adapter**
that keeps the monolith working. The **bulk code-move itself is delegated** — modularize-ops
emits a Structural migration plan and hands it to `oc-migration-ops /oc-migrate execute`
(the real ecosystem mechanism for moving code with verification gates). modularize-ops is
the **decomposition brain + equivalence oracle**, *not* a second code-mover (this is why
its `build` phase output is seam contracts + façades + fixtures + the verifier, not moved
modules — see R3).

### Phase 4 — Verify (`/oc-modularize verify`) — the equivalence proof
**Replay the golden fixtures** against the modularized system and assert semantic
equivalence with the monolith's recorded outputs, boundary by boundary. Plus
data-integrity: row counts, checksums on owned data, no orphaned references across the
new seam. This is the *"perfect split, no data loss"* proof. FAIL → fix or roll back the
extraction. `/oc-modularize diff` shows monolith-behavior vs modularized-behavior per
boundary.

### Phase 5 — Cutover handoff
Hand the verified module set to:
- `oc-migration-ops` (**Structural** type) — the bulk code-move + **live cutover**
  mechanics (dual-write, traffic shift, decommission) via `/oc-migrate execute`.
- `oc-fleet-ops` — **deploy** the resulting containers across the target environment.

The handoff is a **named artifact**, not conversation — see *Handoff contract* below.

## Session Persistence (Checkpoint Protocol)
Mirrors `oc-migration-ops`, because a half-extracted monolith behind a façade is exactly
the orphan risk the protocol guards against.
- **Resume on start:** show `recommendation`, `strategy`, extracted modules + which
  module is next; offer continue / restart / show.
- **Concurrent guard:** **one active modularization per project.** If a checkpoint has
  `status: in_progress`, block a new `/oc-modularize assess|plan` until the current one is
  completed, verified, or explicitly abandoned.
- **`/oc-modularize abandon`:** archive the checkpoint (`.bak`) and warn — *"the monolith
  may be mid-extraction behind a façade; run `/oc-modularize verify` to check current
  health before starting over."*

## Boundaries — what oc-modularize-ops does NOT own
| Concern | Owner |
|---|---|
| Bulk code-move + live cutover (dual-write, traffic shift) | `oc-migration-ops` (Structural `/oc-migrate execute`) |
| Deploying the resulting modules | `oc-fleet-ops` |
| Documenting the existing monolith | `oc-reverse-spec` |
| Greenfield architecture | `oc-app-architect` |
| The engine swap (DB/framework/platform) itself | `oc-migration-ops` |

## Cross-skill wiring
| Reads from | Why |
|---|---|
| `oc-reverse-spec` | current architecture / module map |
| `oc-app-architect` | spec, data model |
| `oc-code-auditor` | coupling hotspots — natural seams |
| `oc-scale-ops` | which areas need independent scaling (a real split driver) |

| Chains to | Why |
|---|---|
| `oc-migration-ops` | emit a Structural migration plan for the move + live cutover |
| `oc-fleet-ops` | deploy the carved-out modules |
| `oc-code-auditor` | audit each extracted module |
| `oc-git-ops` | commit per extraction |

> **Resolves an existing seam:** add a note to `oc-migration-ops`'s *Structural* type
> pointing live, real-traffic decompositions at `oc-modularize-ops` and clarifying the
> deconfliction trigger above. The Structural type stays the home for git-diffable reorgs.

## PM-Tool MCP Integration (parent + per-module child, like migration-ops)
Mirrors `oc-migration-ops`'s parent + step-child mirror; runtime contract deferred to
`oc-integrations-engineer/references/pm-mcp-protocol.md`:
- **Parent ticket** on `/oc-modularize plan`: title *"Modularize: {monolith} → N modules"*,
  type `epic`/`chore` from `pm.yaml`, body = strategy + abort criteria + fixture coverage.
- **Child per module** with state machine
  `plan-pending → in_progress → equivalence-verified → done` (and `blocked` on a failed
  replay), marker `<!-- opchain:oc-modularize-ops:module:<module-id> -->`.
- Records parent + child ids in `skill_state.pm.{parent_ticket, module_tickets[]}`.

## Checkpoint
```jsonc
"progress_table": [
  { "id": "assess",        "label": "Modularization fitness",   "status": "not_started" },
  { "id": "assess-gate",   "label": "★ Should-we? decision",    "status": "not_started" },
  { "id": "characterize",  "label": "Golden fixtures (real)",   "status": "not_started" },
  { "id": "plan",          "label": "Seams + data ownership",   "status": "not_started" },
  { "id": "plan-gate",     "label": "★ Strategy approval",      "status": "not_started" },
  { "id": "extract",       "label": "Extract modules (×N)",     "status": "not_started" },
  { "id": "verify",        "label": "Equivalence + integrity",  "status": "not_started" },
  { "id": "cutover",       "label": "Cutover handoff",          "status": "not_started" }
]
```

```jsonc
"skill_state": {
  "recommendation": "modularize | do-not-modularize",
  "strategy": "parallel-copy | strangler",
  "status": "in_progress",
  "golden_fixtures_path": "modularization/fixtures/",
  "fixture_coverage": { "boundaries_total": 18, "boundaries_with_fixtures": 18 },
  "modules": [
    { "id": "billing", "seam_contract": "modularization/seams/billing.md",
      "owns_data": ["invoices","charges"], "fixtures_captured": true,
      "extracted": true, "equivalence_verified": true }
  ]
}
```

## references/
- `modularization-fitness.md` — when to / when **not** to; the anti-signals.
- `golden-fixtures.md` — real-data capture, PII handling, the coverage gate, the oracle.
- `seam-patterns.md` — façade / strangler / parallel-copy, splitting data ownership.
- `equivalence-verification.md` — fixture replay + data-integrity checks.

## Principles
1. **Be willing to say "don't."** A bad split is worse than a monolith.
2. **Real data or no oracle.** Mock fixtures cannot prove behavior preservation.
3. **No fixture, no split.** Every crossed boundary is characterized before it's cut.
4. **One module at a time, façade always up.** Prod keeps serving throughout.
5. **Equivalence is the gate, not "it compiles."** Replay proves it; nothing else does.
6. **Own the decision, delegate the move.** Code-move + cutover → migration-ops; deploy → fleet-ops.

---

# Skill 3 — `oc-fleet-ops`

## Proposed frontmatter

```yaml
name: oc-fleet-ops
displayName: OC · Fleet Ops
version: 1.7.0
shortDesc: Provision, deploy, and operate one-or-more containers across arbitrary self-managed environments — k8s/Nomad/Compose, IaC, on-prem VMs, GCE. Terraform when it fits, not by default.
phases: [build]
triAgent: false
tryable: true
commands:
  - /oc-fleet
  - /oc-fleet topology
  - /oc-fleet provision
  - /oc-fleet deploy
  - /oc-fleet verify
  - /oc-fleet scale
  - /oc-fleet drain
  - /oc-fleet rollback
  - /oc-fleet status
description: >
  Multi-container / orchestration deployment operator for self-managed infrastructure.
  Declares topology (containers/services × target environment), provisions infra with the
  right IaC tool (Terraform/OpenTofu when it fits, else Ansible/cloud-init/k8s-manifests/
  Helm/Nomad/Compose), rolls the fleet with a rollout strategy, verifies fleet-wide health,
  and operates day-2 (scale/drain/replace/rollback). Specifically lands the modules
  oc-modularize-ops carves out of a monolith. Use for /oc-fleet, "deploy multiple containers",
  "kubernetes", "terraform", "orchestrate containers", "on-prem deployment", "deploy to VMs",
  "self-managed infra", "container fleet". Complements oc-deploy-ops (single-app managed PaaS):
  managed app → deploy-ops; multi-container/self-managed/IaC → fleet-ops.
  Trigger on multi-container / IaC / self-managed deployment.
governance:
  breaking_change_policy: skills/CHANGELOG.md
  last_reviewed: 2026-06-26
  owner: opchain
```

## Mission

Deploy and operate **one-or-more containers across arbitrary, self-managed
environments** — the territory `oc-deploy-ops` routes elsewhere today. It owns
multi-container orchestration (k8s/Nomad/Compose/Swarm), infrastructure-as-code
(Terraform *and its alternatives*), arbitrary environments (on-prem Linux VMs, GCE, bare
metal), and specifically **landing the modules that `oc-modularize-ops` carved out of a
former monolith**.

> Mnemonic: **deploy-ops pushes one app to a platform; fleet-ops stands up and runs a
> fleet on infra you own.** (deploy-ops↔fleet-ops are **peers**, not a chain — the
> orchestrator routes by "managed app vs self-managed fleet," see Boundaries.)

## Operating model — gated ops phases

```
topology ──► provision ──★plan-gate──► deploy ──► verify ──► operate
(what runs    (IaC, tool   (dry-run    (roll the   (fleet-wide  (scale / drain /
 where)        chosen to    NEVER       fleet)      health)      replace / rollback)
               fit the env) auto-apply)
```

### Phase 0 — Topology (`/oc-fleet topology`)
Declare **what runs where**: the set of containers/services, replica counts, the target
environment(s), networking, secrets, and state/volumes. When the input is a module map
from `oc-modularize-ops`, ingest it via the named *Handoff contract* (below) — each
module → one (or more) container(s).

### Phase 1 — Provision (`/oc-fleet provision`) — IaC, *Terraform when it fits*
Stand up the infra. **The tool is chosen to fit the environment — not Terraform by
reflex** (the explicit "Terraform is not always the answer" stance):

| Tool | When |
|---|---|
| **Terraform / OpenTofu** | cloud resources, multi-provider, long-lived infra worth a state file |
| **Ansible / cloud-init** | configuring *existing* VMs, on-prem boxes, no desire for TF state |
| **k8s manifests / Helm / Kustomize** | you already have a cluster |
| **Nomad jobspecs** | a Nomad shop |
| **docker-compose** | single-host, small fleet, dev/staging |

The skill **picks and justifies** (recorded in `skill_state.iac_choice_rationale`).
**IaC apply is gated: dry-run/plan first, never auto-apply** (see R7 — this is the
highest-blast-radius operation in the catalog).

### Phase 2 — Deploy (`/oc-fleet deploy`)
Roll the fleet: build/push images, apply manifests/plans, deploy per-container with a
**rollout strategy** (rolling / blue-green / canary across nodes). Per-node and
per-container granularity — not a single atomic push.

### Phase 3 — Verify (`/oc-fleet verify`)
Health across the **fleet**, not one URL: every container/replica healthy, cross-service
connectivity up, the **new module-boundary calls (from #2) actually resolve**, no node
left behind.

### Phase 4 — Operate (`/oc-fleet scale|drain|rollback`)
Day-2: scale replicas/nodes, cordon+drain a node for maintenance, replace a failed
container, roll the whole fleet back to last-good.

### Failure modes (verify + operate)
A self-managed multi-node fleet has *more* failure surface than a single PaaS app, so
these are first-class, mirroring `oc-deploy-ops` / `oc-monitoring-ops`:
- **Partial rollout** → halt at the configured failure threshold and auto-rollback the
  rolled subset; never march a broken image across the whole fleet.
- **Undrainable node** → escalate (workloads won't reschedule) rather than force-kill.
- **Image-pull / registry-auth failure** → fail the deploy at the pull step, keep prior
  replicas serving.
- **`fleet_health` is tri-state** (`ok | partial | unhealthy`), not a flat `ok` — a fleet
  can be *partially* healthy, which is operationally distinct.

## Scope discipline — first-class environments (like deploy-ops's Platform Matrix)
fleet-ops is broad; it ships 1.7 with a **first-class shortlist** rather than claiming
universal coverage (mirrors `oc-deploy-ops`'s intentionally-short Platform Matrix):

| Environment | 1.7 status |
|---|---|
| docker-compose (single host) | **first-class** |
| Kubernetes (manifests / Helm) | **first-class** |
| Linux VMs (Ansible / cloud-init), on-prem or GCE | **first-class** |
| Nomad | reachable, not first-class |
| Terraform multi-cloud (large) | reachable, not first-class |

A later minor release promotes any of these by adding an IaC recipe + a `/demo` scenario.

## Boundaries — what oc-fleet-ops does NOT own
| Concern | Owner |
|---|---|
| Single app → managed PaaS (Workers/Render/Heroku/Fly/Shuttle) | `oc-deploy-ops` (**peer**, not chain) |
| Continuous prod observability (uptime/errors/SLOs) | `oc-monitoring-ops` |
| Capacity/caching/query strategy + replica **targets** | `oc-scale-ops` (advisory) — fleet-ops **actuates** replica/node counts, it never **decides** them |
| Choosing the target platform | `oc-stack-forge` |
| The engine swap / platform migration | `oc-migration-ops` |

## Cross-skill wiring
| Reads from | Why |
|---|---|
| `oc-modularize-ops` | the module set to deploy (via the Handoff contract) |
| `oc-stack-forge` | target infra / platform decision |
| `oc-scale-ops` | replica / capacity **targets** (fleet-ops applies; scale-ops decides) |
| `oc-app-architect` (`07-devops.md`) | deploy pattern intent |

| Chains to | Why |
|---|---|
| `oc-monitoring-ops` | post-deploy observability across the fleet |
| `oc-git-ops` | commit the IaC |

> **Resolves an existing seam:** deploy-ops's Platform Matrix currently routes bare-metal
> to *oc-migration-ops* (verbatim: *"bare-metal needs oc-migration-ops, not
> oc-deploy-ops"*). S5 **edits that row** to re-point self-managed/multi-node/IaC deploys
> at `oc-fleet-ops` — a behavioural change. It also registers `deploy-ops ↔ fleet-ops` as
> **peers** in the orchestrator so routing is unambiguous when a project has both
> checkpoints (managed app → deploy-ops; self-managed fleet → fleet-ops).

## PM-Tool MCP Integration (fleet-deploy ticket, like deploy-ops)
Mirrors `oc-deploy-ops`'s per-env deploy ticket; runtime contract deferred to
`oc-integrations-engineer/references/pm-mcp-protocol.md`:
- **Fleet-deploy ticket** per `environment + commit` on `/oc-fleet deploy`, marker
  `<!-- opchain:oc-fleet-ops:fleet-deploy:<env>:<sha> -->`, body = topology summary +
  rollout strategy + IaC tool.
- **Per-event updates:** `rolled` / `partial-halt` / `rolled-back` transitions, each with
  its own marker; records `skill_state.pm.deploy_tickets[]`.
- No PM-MCP availability → deploy proceeds; updates deferred (`pm_deferred_actions[]`).

## Checkpoint
```jsonc
"progress_table": [
  { "id": "topology",   "label": "Containers × environment",  "status": "not_started" },
  { "id": "provision",  "label": "Provision infra (IaC)",     "status": "not_started" },
  { "id": "prov-gate",  "label": "★ Dry-run / plan approval",  "status": "not_started" },
  { "id": "deploy",     "label": "Roll the fleet",            "status": "not_started" },
  { "id": "verify",     "label": "Fleet-wide health",         "status": "not_started" },
  { "id": "operate",    "label": "Day-2 (scale/drain/roll)",  "status": "not_started" }
]
```

```jsonc
"skill_state": {
  "environment": "kubernetes | nomad | compose | vms | gce | baremetal",
  "iac_tool": "terraform | ansible | helm | nomad | compose",
  "iac_choice_rationale": "existing cluster → Helm; no new TF state needed",
  "containers": [
    { "name": "billing", "image": "billing:abc123", "replicas": 3, "healthy": 3 }
  ],
  "nodes": ["node-a","node-b"],
  "rollout_strategy": "rolling",
  "last_deploy": "2026-07-xxTxx:xxZ",
  "rollback_available": true,
  "fleet_health": "ok | partial | unhealthy"
}
```

## references/
- `topology-design.md` — containers × environment, networking, secrets, state.
- `iac-selection.md` — Terraform-vs-alternatives decision matrix.
- `rollout-strategies.md` — rolling / blue-green / canary on self-managed infra.
- `fleet-verification.md` — fleet-wide health, cross-service + module-boundary checks.

## Principles
1. **Terraform is a tool, not the answer.** Pick IaC to fit the environment; justify it.
2. **A fleet is not one URL.** Verify every replica and every cross-service edge.
3. **Roll, don't big-bang.** Rolling/blue-green/canary, per-node granularity.
4. **Dry-run before apply, always.** IaC apply is the catalog's highest-blast-radius act.
5. **30-second rollback for the whole fleet.** Record last-good every deploy.
6. **deploy-ops's peer, not its rival.** Managed PaaS → deploy-ops; self-managed fleet → here.

---

## Handoff contract — the modularize → migration → fleet chain

The orchestrator passes context through **checkpoints, not conversation** (orchestrator.md
§3, *Context Passing*). So the chain needs a named payload, not "ingest it directly":

- **oc-modularize-ops writes** `skill_state.modules[]` with, per module:
  `{ id, seam_contract, owns_data[], image_hint, equivalence_verified }`.
- **oc-migration-ops reads** that set to build the Structural cutover plan (which module
  moves, what data it owns, dual-write boundaries).
- **oc-fleet-ops `topology` reads** the same `modules[]` to seed containers
  (`module.id → container`, `module.image_hint → image`). The module map is the shared,
  named artifact (`modularization/module-map.json`), referenced by both downstream skills.

---

# Ecosystem changes required for 1.7

These are the touch-points a future build sprint must hit (learned from how 1.5/1.6
landed, and **fact-checked against the live repo in the verification pass**):

1. **Catalog count 24 → 27; lockstep bump every skill → `1.7.0`.** opchain versions in
   lockstep — one minor moves the whole catalog (`skills/CHANGELOG.md`). **Bump the
   hard-coded count strings too:** `skills/orchestrator.md` (the *"v1.6 takes the catalog
   to 24 skills"* line in §8), `skills/README.md` inventory, `mirror/README.md` (public
   surface), and any site copy. Nothing validates these prose counts — they go stale
   silently.
2. **`skills/orchestrator.md`** (the shared protocol bundled in every skill's
   `references/`) — add the three to (use the **real** headers):
   - **§2 Pipeline Map** — the ASCII map *and* the **"Upstream/Downstream Map"
     subsection** inside it (it's a `###` under §2, not a standalone section).
   - **§4 Novice Mode → "Smart Routing Table" subsection** (the routing table is a `###`
     inside §4 Novice Mode, not §4 itself).
   - **§7 Skill Descriptions** and **§8 Ecosystem Awareness** — and add `oc-signal-forge`
     to §8's **Tri-agent skills** list (Designer/Builder/Evaluator), plus the new
     `app-architect Phase 2 → oc-signal-forge` auto-invocation.
   - New edges to register: `oc-app-architect (Phase 2 · 08-analytics.md) → oc-signal-forge`;
     `oc-signal-forge → oc-dash-forge`; `oc-signal-forge → oc-monitoring-ops` (freshness
     SLA); `oc-modularize-ops → oc-migration-ops (Structural) → oc-fleet-ops`;
     `oc-modularize-ops → oc-fleet-ops`; `oc-fleet-ops → oc-monitoring-ops`;
     **`oc-deploy-ops ↔ oc-fleet-ops` as peers** (and re-point the deploy-ops bare-metal
     row from migration-ops to fleet-ops); fix the `oc-scale-ops` row so the
     scale-ops→fleet-ops *targets* edge is captured (scale-ops stays advisory; fleet-ops
     actuates).
3. **Feature flags** (`src/lib/flags/registry.js`) — one `skills.registry.<id>.enabled`
   per new skill dir: `oc-signal-forge`, `oc-modularize-ops`, `oc-fleet-ops`. The build
   fails on drift (`scripts/gen-skills-catalog.mjs` asserts each `SKILL.md`'s required
   flags/verbs have a registry entry; `scripts/gen-flags.mjs` mirrors the registry into
   the typed `site/src/lib/flags/registry.ts`).
4. **Catalog validation** (`scripts/gen-skills-catalog.mjs`) — directory name must equal
   `frontmatter.name`; required frontmatter present; **`phases` must be a subset of
   `VALID_PHASES = {foundation, plan, build, ai-native}`** (this is why signal-forge is
   `[build]`, not `[design, build]` — `design` would throw). The Astro site reads
   `skills/` directly via `site/src/content.config.ts`.
5. **`skills/CHANGELOG.md`** — a `[1.7.0] — "Seams & Signals"` entry (additive ⇒ MINOR;
   no documented cross-skill contract is *broken* — but note the deploy-ops bare-metal
   re-point in §2 is a *routing* change worth calling out).
6. **`.claude/skills/<id>` must be a SYMLINK** → `../../skills/<id>` (not a copy). CI's
   *"Verify .claude/skills/ has no drift"* step hard-fails on any non-symlink entry
   (this is the exact check the stray `*.zip` artifacts tripped). **Add the symlink-drift
   check to the S1 gate** as a distinct gate from `gen-catalog`.
7. **Public mirror** — `skills/` auto-mirrors to `asfbay-bit/opchain-skills`
   (`.github/workflows/mirror-public.yml` on push to `main` touching `skills/`); the
   three new skills + the bumped `mirror/README.md` count propagate on the next sync.
8. **Site release surfaces** — drive from `skills/oc-release-ops/references/site-release-surfaces.md`;
   the `scripts/check-release-surfaces.mjs` guard asserts specific surfaces:
   the `CURRENT_RELEASE_HREF` regex + changelog hero in `site/src/pages/changelog.astro`,
   the styleguide version **Badge**, and `site/src/data/roadmap-static.ts`. Name those in
   the S6 checklist (not a vague "/changelog v1.7 card").
9. **Checkpoint protocol** — **no wire bump needed**, but the `progress_table` shape is
   **resolved, not deferred**: it is a JSON **array of `{id,label,status}` objects** (per
   `oc-monitoring-ops` and the schema), which all three briefs above now use. The signal
   catalog and golden-fixture index live in `skill_state` + on disk.

---

# Build roadmap (for a future 1.7 build — not part of this plan task)

Six sprints, mirroring how 1.6 was decomposed:

| Sprint | Goal | Deliverables |
|---|---|---|
| **S1 — Scaffolds + wiring** | three skill dirs exist, validation green | `skills/<id>/SKILL.md` frontmatter for all 3 (valid `phases`); bundled `references/orchestrator.md` + `checkpoint-protocol.md`; flags registry entries; **`.claude/skills/<id>` symlinks**; **orchestrator.md ecosystem edits** (real headers per §2 above). Gate: `gen-catalog` sees 27, `astro check` clean, flags mirror clean, **symlink-drift check green**. |
| **S2 — oc-signal-forge** | first skill ships value (most standalone) | full body + 3 references + **`TRYIT.md`** + PM-MCP section; Designer/Builder/Evaluator loop. |
| **S3 — oc-modularize-ops** | decomposition brain | full body + 4 references (fitness, golden-fixtures, seam-patterns, equivalence) + **`TRYIT.md`** + PM-MCP + Session-Persistence. |
| **S4 — oc-fleet-ops** | self-managed deploy | full body + 4 references (topology, iac-selection, rollout, verification) + **`TRYIT.md`** + PM-MCP + failure-modes; first-class-environment shortlist. |
| **S5 — cross-skill ripples** | the new edges are real, not just declared | app-architect Phase 2 → signal-forge; **edit the deploy-ops Platform-Matrix bare-metal row → fleet-ops** + register the deploy-ops↔fleet-ops peer; migration-ops Structural-type deconfliction note → modularize-ops; dash-forge consumes signal-forge; monitoring-ops enforces signal `freshness_sla` + fleet health. |
| **S6 — release** | catalog @ 1.7.0, surfaces live | lockstep bump 24→27 @ `1.7.0`; `CHANGELOG [1.7.0]`; bump the prose count strings (item 1); site roadmap + changelog hero + styleguide badge per `site-release-surfaces.md`; demo/showcase scenarios; `check-release-surfaces` green. |

**TRYIT.md reconciliation (S2–S4):** all three declare `tryable: true`, but
modularize-ops (real prod fixtures) and fleet-ops (real k8s/VMs) are hard to demo in a
marketing sandbox. Each `TRYIT.md` must name the **synthetic stand-in** for real
data/infra; if a skill can't be meaningfully tried, set `tryable: false` honestly rather
than ship a dead Try-It surface.

**Sequencing:** S2/S3/S4 are *authored sequentially* by a single builder (the roadmap
isn't actually parallel — see R5). S5 depends on all three. S6 is last.

---

# Open questions & risks

| # | Risk / question | Recommendation |
|---|---|---|
| R1 | **Golden fixtures from real prod data = PII/secrets exposure.** | `oc-modularize-ops` Phase 1 loops `oc-security-auditor`; fixtures default to redact/synthesize-preserving-shape; `modularization/fixtures/` is gitignored by default with explicit opt-in. **Hard requirement.** |
| R2 | **fleet-ops scope creep** — k8s + Nomad + VMs + Terraform + bare metal is enormous. | Ship the **first-class shortlist** (compose / k8s / Linux-VMs); mark the rest "reachable, not first-class," like deploy-ops's Platform Matrix. |
| R3 | **Does modularize-ops move code?** | **No.** It produces seam contracts + façades + the fixture oracle + the equivalence verifier (its `build` output), and **delegates the bulk code-move + cutover to `oc-migration-ops` Structural `/oc-migrate execute`**. Mission, Phase 3, frontmatter, and this row now agree. |
| R4 | **signal-forge vs `08-analytics.md` overlap.** | Role split: `08-analytics.md` is the *plan*; `oc-signal-forge` is the *executor* the plan chains to (app-architect Phase 2). |
| R5 | **Three skills in one release — and the roadmap isn't actually parallel.** | **Plan of record: phased.** Ship `oc-signal-forge` as **1.7** (most standalone, immediate value, clean justification). Ship `oc-modularize-ops` + `oc-fleet-ops` as **1.7.1** — they're a chain with little standalone value and fleet-ops depends on the deploy-ops re-point. Don't lean on raw skill-count precedent without per-skill effort estimates. |
| R6 | **Theme name.** "Seams & Signals" proposed. | Confirm at release time via `oc-release-ops` (it owns the changelog theme). |
| R7 | **fleet-ops IaC blast radius** — `terraform apply` against real infra is the most dangerous act any opchain skill performs. | **Mandatory dry-run/plan gate, never auto-apply** (Phase 1 `prov-gate`); explicit target-environment confirmation; tri-state `fleet_health`; partial-rollout auto-rollback. Baked into the brief, not just a principle. |

---

# Appendix — Verification corrections (2026-06-26)

This plan was revised after an adversarial pass: 5 independent reviewers fact-checked
every boundary and ecosystem claim against the live repo (462k tokens, 63 tool calls).
All five returned *needs-revision*; the bones held, the details didn't. What changed:

- **Build-breaker fixed:** `oc-signal-forge` `phases: [design, build]` → `[build]`
  (`design` ∉ `gen-skills-catalog.mjs` `VALID_PHASES`; the S1 gate would have hard-failed).
- **Fabricated quote removed:** the deploy-ops *"needs a migration-ops/this-shaped skill /
  vacated territory"* framing was wrong. Real text: *"bare-metal needs oc-migration-ops,
  not oc-deploy-ops."* fleet-ops's reason-to-exist is now a **behavioural re-point** of an
  *assigned* row, justified by migration-ops lacking a provision/topology/fleet surface.
- **PM-Tool MCP Integration added** to all three briefs (17 sibling skills carry it).
- **`progress_table` rewritten** from arrow-strings to the canonical array-of-objects in
  all three; R9 marked resolved.
- **`.claude/skills/` corrected** to symlink-only (CI hard-fails on a copy) + S1 gate.
- **modularize-ops vs migration-ops Structural** sharpened with a routing trigger +
  deconfliction note (Structural already auto-detects "split the monorepo"); the
  "lightweight cousin" framing dropped; Session-Persistence + concurrent-guard + `abandon`
  defined; the code-move contradiction (R3) resolved by delegating to migration-ops.
- **fleet-ops** scale-ops seam fixed (actuates, never decides), deploy-ops registered as a
  **peer**, failure-modes + R7 IaC-blast-radius added.
- **Handoff contract** for the modularize→migration→fleet chain made an explicit named
  artifact (`modularization/module-map.json`).
- **orchestrator.md references corrected** (§4 *Novice Mode* → Smart Routing subsection;
  Upstream/Downstream *inside* §2) + the hard-coded "24 skills" count strings enumerated.
- **Build roadmap** TRYIT.md reconciled across S2–S4; "parallel" claim corrected to
  sequential; R5 phased-release promoted to plan of record.

*End of 1.7 plan. Next step when you're ready to build: `oc-release-ops /oc-release plan`
to slot signal-forge as 1.7, then `oc-app-architect /oc-scaffold` per Sprint 1.*
