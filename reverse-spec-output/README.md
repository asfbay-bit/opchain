# Reverse-Spec Output — opchain.dev

Generated by the `reverse-spec` skill against this repo on 2026-04-17.

## Contents

```
reverse-spec-output/
├── ORIENTATION.md           Quick summary of what this project is and scope of analysis
├── spec/
│   ├── 00-project-overview.md    What opchain is (product + site), audience, delivery
│   ├── 01-tech-stack.md          Runtime, KV, bundler, frontend, dependencies
│   ├── 02-architecture.md        Worker + site architecture AND skills-ecosystem topology
│   ├── 03-security-auth.md       Session tokens, rate limits, CORS, security headers
│   ├── 04-integrations.md        Linear GraphQL + Anthropic Messages API
│   ├── 06-testing.md             Current state (none) + recommended baseline
│   └── 07-devops.md              Deploy pipeline, environments, secrets, observability
├── design/
│   ├── design-system.md          Tokens, typography, spacing, layout patterns, breakpoints
│   └── component-inventory.md    ~18 UI patterns with states and gaps
├── stack-forge-audit.md          Typed pipeline link-by-link status
├── gap-analysis.md               Prioritized list (HIGH / MED / LOW)
└── tri-dev-ready/
    └── spec.md                    App-architect-ready spec describing the project as it exists
```

## What was skipped (and why)

| Doc | Why skipped |
|---|---|
| `05-monetization.md` | No payment / subscription code in the repo |
| `08-analytics.md` | No analytics or tracking code |
| `09-documentation-plan.md` | No docs infra beyond markdown files; in-repo docs are CLAUDE.md + README.md + per-skill SKILL.md |
| `10-cost-estimate.md` | Folded into `07-devops.md` — minimal cost surface given Workers Free tier + pinned Haiku model |

## Pipeline readiness

| Next step | Readiness |
|---|---|
| `app-architect /roadmap` (plan new features) | **READY** — spec baseline exists |
| `stack-forge` retroactive audit | **DONE** — see `stack-forge-audit.md` |
| `tri-dev` / app-architect build | **READY** — `tri-dev-ready/spec.md` is populated |
| `code-auditor /audit full` | **RECOMMENDED** — prioritize the HIGH items in `gap-analysis.md` first |

## Top 3 gaps to resolve first

1. **Remove hardcoded fallback HMAC secret** (`src/opchain-try.js` L285, L306) — prevents forgeable session tokens if `DEPLOY_API_TOKEN` is unset in prod.
2. **Add CI + minimum test suite** — no guardrails currently stop a broken build from deploying.
3. **Add a staging environment and a KV preview id** — every change today goes straight to production and may touch prod KV during `wrangler dev`.

See `gap-analysis.md` for the full prioritized list.
