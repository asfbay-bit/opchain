# Reverse-Spec Output — opchain.dev

Generated by the `reverse-spec` skill against this repo on 2026-04-17;
selectively refreshed on 2026-04-28 to reflect the Try-It removal,
Sprint 6 Astro cutover, Sprint 7c CSP, CI suite, and staging
environment.

## Contents

```
reverse-spec-output/
├── ORIENTATION.md           Quick summary of what this project is and scope of analysis [REFRESHED 2026-04-28]
├── spec/
│   ├── 00-project-overview.md    What opchain is (product + site), audience, delivery [REFRESHED 2026-04-28]
│   ├── 01-tech-stack.md          Runtime, KV, Astro 5, Tailwind 4, dependencies [REFRESHED 2026-04-28]
│   ├── 02-architecture.md        Worker + site architecture AND skills-ecosystem topology [REFRESHED 2026-04-28]
│   ├── 03-security-auth.md       No auth, CSP nonce, rate limits, lead PII [REFRESHED 2026-04-28]
│   ├── 04-integrations.md        Linear GraphQL + PostHog [REFRESHED 2026-04-28]
│   ├── 06-testing.md             Vitest + Playwright + axe + LHCI in CI [REFRESHED 2026-04-28]
│   └── 07-devops.md              Manual deploy posture, staging, version stamp [REFRESHED 2026-04-28]
├── design/                       Pre-Sprint-6 component inventory — STALE, not refreshed in this pass
│   ├── design-system.md
│   └── component-inventory.md
├── stack-forge-audit.md          Pre-Sprint-6 typed pipeline audit — STALE, not refreshed
├── gap-analysis.md               Current prioritized punch list (HIGH / MED / LOW) [REFRESHED 2026-04-28]
└── tri-dev-ready/
    └── spec.md                   Pre-Sprint-6 app-architect-ready spec — STALE, not refreshed
```

## What was skipped (and why)

| Doc | Why skipped |
|---|---|
| `05-monetization.md` | No payment / subscription code in the repo |
| `08-analytics.md` | Folded into `04-integrations.md` (PostHog section) |
| `09-documentation-plan.md` | No docs infra beyond markdown files; in-repo docs are CLAUDE.md + README.md + per-skill SKILL.md + this folder |
| `10-cost-estimate.md` | Folded into `07-devops.md` — minimal cost surface given Workers Free tier and no LLM-token budget after Try-It removal |

## What was NOT refreshed in this pass

The 2026-04-17 outputs below were generated against the pre-Sprint-6
codebase (vanilla HTML in `public/*.html`, Try-It chat, no Astro). They
describe a system that no longer exists as documented and should be
regenerated rather than spot-edited:

| Doc | Why it's stale |
|---|---|
| `design/design-system.md` | Tokens were extracted from the hand-written `public/styles.css`; the site is now Tailwind 4 + `site/src/styles/{global,tokens}.css` |
| `design/component-inventory.md` | Inventoried `public/*.html` patterns; the site is now Astro components in `site/src/components/` |
| `stack-forge-audit.md` | Audited a typed pipeline that didn't include Zod, Vitest, or Playwright — all of which now exist |
| `tri-dev-ready/spec.md` | Built from the stale `00`/`02` docs |

A follow-up `/rev-design` and `/rev-stack` run would refresh those.

## Pipeline readiness

| Next step | Readiness |
|---|---|
| `app-architect /roadmap` (plan new features) | **READY** — refreshed spec baseline exists |
| `stack-forge` retroactive audit | **STALE** — needs regeneration via `/rev-stack` |
| `tri-dev` / app-architect build | **STALE** — `tri-dev-ready/spec.md` derives from the old overview |
| `code-auditor /audit full` | **RECOMMENDED** — prioritize the HIGH item in `gap-analysis.md` first |

## Top gap to resolve first

The 2026-04-17 top-3 gaps (hardcoded HMAC fallback, no CI, no staging
env) are **all resolved**. The single HIGH that remains:

1. **Provision the `NOTIFY` KV namespace IDs** in `wrangler.jsonc`
   (both prod and staging are empty strings today). Until that's
   done, every `/api/notify` submission is silently dropped — the
   handler is written to degrade open. One `wrangler kv namespace
   create` per env, plus `--preview` for local dev, plus a redeploy.

See `gap-analysis.md` for the full list, including the MED items
carried forward from the original run (lead retention TTL, Tailwind
`unsafe-inline` on `style-src`, CORS allow-list audit, Linear retry +
User-Agent).
