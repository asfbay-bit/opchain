# opchain.dev Redesign — Roadmap

Generated by the `app-architect` skill (`/roadmap`) on 2026-04-17, starting
from the `reverse-spec-output/` baseline.

## Contents

| Doc | Purpose |
|---|---|
| `00-approach.md` | Why redesign; success criteria; explicit assumptions to confirm or redirect; non-goals; gate protocol |
| `01-stack-decision.md` | Stack-forge auto-invoke — platform, framework, language, validation, styling, tests, CI |
| `02-sprint-plan.md` | 7 sprints with features, deliverables, test requirements, Definition of Done, effort |

## TL;DR

- **Seven sprints, ~82 Claude hours + ~14 user hours, ~2–3 weeks.**
- **Stack:** Cloudflare Workers (kept) + Astro 5 + TypeScript + Zod + Tailwind 4.
- **Order:** foundation → single source of truth for skills → design system → page rebuilds → try-it rewrite → hardening → cutover.
- **Every sprint resolves specific gaps from `reverse-spec-output/gap-analysis.md`.**

## Sprint headlines

| # | Name | Outcome |
|---|---|---|
| 0 | Foundation | CI + staging env + remove fallback secret + version stamp |
| 1 | Single source of truth | One catalog in `skills/`, drives site and Try-It |
| 2 | Design system v2 | Tokens + component library + styleguide page |
| 3 | Page rebuilds | Real content on every page; per-skill detail pages |
| 4 | Try-It rewrite | TS + Zod + retries + per-request IDs + safer markdown |
| 5 | Hardening | CSP + lead TTL + structured logs + Dependabot |
| 6 | Cutover | Replace `public/` + `src/` with Astro output; smoke tests |

## Gate protocol

This roadmap sits at the `sprint-plan-approved` gate in app-architect's
pipeline (`skills/app-architect/SKILL.md` L269–L270). It does **not** start
a build — it waits for user approval or redirect.

To move forward: approve the assumptions in `00-approach.md`, then
`/scaffold` initializes the new `site/` directory and Sprint 0 begins.

## How this relates to the reverse-spec

Every HIGH and MED gap from `reverse-spec-output/gap-analysis.md` is mapped
to a specific sprint:

| Gap | Severity | Sprint |
|---|---|---|
| H1 Hardcoded fallback secret | HIGH | 0 |
| H2 No CI / no tests | HIGH | 0 |
| H3 Single environment | HIGH | 0 |
| H4 No input validation | HIGH | 4 |
| M1 KV prod in dev | MED | 0 |
| M2 Catalog drift | MED | 1 |
| M3 Stub pages | MED | 3 |
| M4 XSS via markdown renderer | MED | 4 |
| M5 Missing security headers | MED | 5 |
| M6 Lead data no TTL | MED | 5 |
| M7 Hardcoded Linear IDs | MED | 4 |
| M8 Hardcoded Anthropic model | MED | 4 |
| M9 Typed pipeline gap | MED | 1 + 4 |
| M10 No skill versioning | MED | 1 |

LOW items are folded into adjacent sprints or explicitly deferred in
`02-sprint-plan.md`.
