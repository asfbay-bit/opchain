# Revised Roadmap — Sprint 7 and Beyond

Generated by `/app-architect` on 2026-04-19, after a full re-audit of `main`.
Sits at the `sprint-plan-approved` gate (per `skills/app-architect/SKILL.md`
L269–L270): **awaiting your approval before any build starts.**

This document supersedes the "Suggested next sprint" section of
`roadmap/03-post-launch.md` (lines 122–134). The original Sprints 0–6 are
shipped — see `03-post-launch.md` for that audit.

---

## Readout — what's true on `main` today

Verified against the working tree at HEAD `bf3ebdf`:

| Area | State | Source of truth |
|---|---|---|
| Sprints 0–6 | Shipped | `roadmap/03-post-launch.md` |
| `/in-action`, pipeline SVG, content audit | Shipped post-cutover | git log `1818c4a`..`a712d5c` |
| Typed client analytics wrapper | Shipped | `site/src/lib/analytics.ts`, `ac5614f` |
| Launch checklist artifact | Shipped | `checklists/launch-checklist.md` |
| PostHog dashboards docs | Shipped | `docs/analytics.md` |
| Skill version pills | **Already shipped** (item #5 in `03-post-launch.md` "what else") | `site/src/pages/skills/[id].astro:46`, `index.astro:88` |
| Playwright e2e harness | **Not present** — no `@playwright/test` in either `package.json`, no `site/tests/e2e/` | verified |
| Axe a11y assertions | Not present | verified |
| Lighthouse CI gate | Not present | no `@lhci/cli`, no Lighthouse step in `.github/workflows/` |
| CSP `script-src` | Still includes `'unsafe-inline'` | `src/index.js:90` |
| Per-page OG images | Single global `/og-image.png` | `site/src/layouts/Base.astro:28` |
| Dependabot | Configured | `.github/dependabot.yml` (PR-landing hygiene unverified) |
| Worker handler in TS | Still JS (intentional defer) | `src/opchain-try.js` |

So the gap between the roadmap and reality is narrower than
`03-post-launch.md` implies — version pills are already done, content audit
pass 1 already landed, the analytics wrapper is wired. What's left is
genuinely the **quality-gate scaffolding** the original Sprints 2/3/4/5
deferred and the **CSP tighten** flagged on launch day.

---

## Sprint 7: Quality Gates & CSP Tighten

**Goal:** Ship the test-and-quality scaffolding that Sprints 2–5 promised
but didn't include, then close the last security tidy from launch day. After
Sprint 7, regression risk on the Astro site drops to "CI catches it" rather
than "we hope someone clicks every page."

**Total effort:** ~14 CLAUDE h / ~1 USER h. Calendar: 2–3 build sessions.

### Sub-sprint 7a — Playwright e2e harness (~7 h Claude)

#### Features (closes original deferred items)
- Sprint 2 DoD: styleguide renders on dark theme.
- Sprint 3 DoD: route 200s, filter behaviour, mermaid as inline SVG, Axe.
- Sprint 4 DoD: email submit reveals chat UI; counter decrements.
- Sprint 5 DoD: consent banner gates PostHog.

#### Deliverables
- Add `@playwright/test` to `site/package.json`. Add `npm run test:e2e` script.
- Create `site/playwright.config.ts` — single Chromium project, `webServer` boots `npm run preview` against the real Astro build.
- Seed `site/tests/e2e/`:
  - `routes.spec.ts` — every top-level route (`/`, `/architecture`, `/install`, `/skills`, `/skills/app-architect`, `/in-action`, `/tryit`, `/privacy`, `/styleguide`) returns 200 and renders its `h1`.
  - `redirects.spec.ts` — `/architecture.html`, `/skills.html`, `/install.html`, `/tryit.html` 301 to canonical (complements existing Vitest mocks of `ASSETS`).
  - `filter.spec.ts` — `/skills` phase filter reduces card count; an empty filter restores it.
  - `tryit.spec.ts` — email submit reveals the chat input; sending a starter prompt streams text; remaining-counter decrements. Mocks `/api/try/*` so tests don't burn Anthropic credits.
  - `consent.spec.ts` — fresh storage shows banner; decline → no `window.posthog`; accept → SDK loads; opt-out from footer resets state.
- Wire `npx playwright test --reporter=line` into `.github/workflows/ci.yml` after the Vitest step. Cache the Playwright browser install.
- Skip visual-regression snapshots — too brittle without dedicated infra; backlog it.

#### Test Requirements
- Five spec files above, each green locally and in CI.
- CI run time budget: e2e step ≤ 90 s on cold cache, ≤ 30 s warm.

#### Definition of Done
- `npm run test:e2e` green on a clean clone.
- CI shows the new step in PR checks; failures block merge.
- README documents how to run e2e locally.

#### Dependencies
None — runs against the existing Astro build.

#### Estimated Effort
CLAUDE: 7 | USER: 0

---

### Sub-sprint 7b — Lighthouse + Axe quality gates (~5 h Claude)

#### Features (closes original deferred items)
- Sprint 3 thresholds: Performance ≥ 95, Accessibility ≥ 95 on `/` and `/skills`.
- Sprint 5 threshold: Best Practices ≥ 95.
- Sprint 6 threshold: all four (Perf, A11y, Best Practices, SEO) ≥ 95.

#### Deliverables
- Add `@axe-core/playwright` to `site/`. Extend `routes.spec.ts` so every route asserts `expect(violations).toEqual([])`. Disable specific rules with inline reason comments — never silently.
- Add `@lhci/cli` GitHub Action `.github/workflows/lighthouse.yml`. Triggered on `pull_request` and on `push` to `main` after staging deploy.
- Run against staging URL post-deploy; budgets file at repo root (`lighthouserc.json`):
  - `/` and `/skills`: Performance ≥ 0.95, Accessibility ≥ 0.95, Best Practices ≥ 0.95, SEO ≥ 0.95.
  - `/in-action` (heaviest page): Performance ≥ 0.85 — relaxed because of mock chat sequences; documented in the budget file.
- Action surfaces a markdown summary in the PR check output (LHCI's built-in `temporary-public-storage` mode is fine to start; no Lighthouse server required).

#### Test Requirements
- Lighthouse run completes within action time budget (≤ 5 min).
- Forcibly degrading a route (e.g. dropping a stub `<img>` without `alt`) makes the action fail — proves the gate works.

#### Definition of Done
- LHCI passes against current production.
- Axe assertion fails on intentional regression in a draft PR; passes after fix.
- Thresholds documented in `lighthouserc.json` with comments.

#### Dependencies
- Sub-sprint 7a (`routes.spec.ts` exists for the Axe extension).
- A reachable staging URL (already true via `.github/workflows/deploy.yml`).

#### Estimated Effort
CLAUDE: 5 | USER: 0

---

### Sub-sprint 7c — CSP nonce migration (~2 h Claude)

#### Features (closes the CSP gap from `03-post-launch.md` "what else", #1)
- Drop `'unsafe-inline'` from `script-src` so CSP reviewers stop flagging the site.

#### Deliverables
- Worker generates a per-request nonce in `src/index.js` for HTML responses; injects it into the `Content-Security-Policy` header (`script-src 'self' 'nonce-{n}' https://*.i.posthog.com https://static.cloudflareinsights.com`).
- Astro `Base.astro` reads the nonce from the request via a small middleware/header pass-through and stamps every inline `<script>` with `nonce="{n}"`. The theme-init FOUC script is the only inline script today; verify with `grep`.
- Drop `'unsafe-inline'` from `script-src`.
- Keep `'unsafe-inline'` on `style-src` for now (Tailwind utility classes occasionally inject inline style attributes; nonce-on-style is more invasive than the value adds).

#### Test Requirements
- Vitest: header includes `nonce-` token, no `unsafe-inline` on `script-src`.
- Playwright (extends `routes.spec.ts`): no CSP violations in console for `/`, `/tryit`, `/in-action`.
- Manual: theme switcher still works without FOUC on dark + light first paint.

#### Definition of Done
- `script-src` does not contain `'unsafe-inline'`.
- Console is clean of CSP violations on every route covered by `routes.spec.ts`.
- `securityheaders.com` rating for opchain.dev ≥ A (was A− because of the inline-script flag).

#### Dependencies
- Sub-sprint 7a (Playwright in place to assert console cleanliness).

#### Estimated Effort
CLAUDE: 2 | USER: 1 (verify FOUC behaviour on staging in both themes before promoting to prod)

---

### Sub-sprint 7d — Staging-first promotion flow (~3 h Claude)

#### Features (formalises Sprint 0 staging + closes a launch-day process gap)

- Makes "push to main → staging; human-verified promote to production" the
  codified deploy path, not an informal convention.
- Removes the risk that a `workflow_dispatch` with `environment=production`
  bypasses the staging smoke entirely (current `deploy.yml` production job
  has `needs: []`).

#### Pre-flight (one-time, user-side — not code)

- **DNS CNAME** for `staging.opchain.dev → opchain-staging.4fstpkkw72.workers.dev`.
  Without it `wrangler deploy --env staging` succeeds but the custom hostname
  never binds and the Worker is unreachable (`*.workers.dev` is blocked
  account-wide with `host_not_allowed`).
- **Staging secrets** on Cloudflare — `wrangler secret put <name> --env staging`
  for each of `DEPLOY_API_TOKEN`, `ANTHROPIC_API_KEY`, `LINEAR_API_KEY`, and
  (optional) `POSTHOG_PROJECT_API_KEY`. Production secrets do **not** carry
  over to the staging environment. Without `DEPLOY_API_TOKEN` the Try-It
  endpoints return 503 on staging by design.
- **GitHub environment** `production` configured with required reviewers so
  the promote workflow pauses for a manual approval click.

No new Worker is created in code — `wrangler deploy --env staging` provisions
the `opchain-staging` Worker on Cloudflare on first run, using the config
already in `wrangler.jsonc env.staging`. Dedicated KV namespace
(`6156bc3d5a5c4bd780f3d2667950e703`) also already exists.

#### Deliverables

- **New workflow** `.github/workflows/promote.yml`:
  - `workflow_dispatch` only; input = `sha` (commit to promote).
  - Job 1 (`verify-staging`): hits `https://staging.opchain.dev/` and
    asserts `X-Opchain-Version` matches the input SHA. Fails if staging is
    serving a different (or stale) build — guarantees staging has seen this
    exact code.
  - Job 2 (`promote`): checks out the input SHA, builds with
    `OPCHAIN_VERSION=<sha>`, deploys with `wrangler deploy` (production),
    runs `scripts/smoke.sh` against prod. Scoped to GitHub `environment:
    production` so reviewer approval is required before the deploy step
    runs.
- **`.github/workflows/deploy.yml` simplification**:
  - Renamed to "Deploy to staging".
  - `production` job removed (moved to `promote.yml`).
  - `workflow_dispatch` kept but without the `environment` choice input —
    it just re-runs a staging deploy.
  - `concurrency.group` simplified to `deploy-staging`.
- **`package.json`** — new `promote` script:
  ```
  git fetch origin main && gh workflow run promote.yml -f sha=$(git rev-parse origin/main)
  ```
  One command kicks off a prod promotion of whatever's currently on `main`
  (assumes `main` has already auto-deployed to staging via `deploy.yml`).
- **`CLAUDE.md`** — Deployment section updated with the canonical flow:
  merge to main → auto-staging → verify staging → `npm run promote` (or
  GitHub UI) → approval gate → production.

#### Test Requirements

- Dry-run promote workflow with a SHA that's **not** on staging → the
  `verify-staging` job fails before any deploy occurs (proves the gate works).
- Dry-run promote workflow after a staging deploy completes with that SHA →
  `verify-staging` passes; `promote` job pauses at the GitHub environment
  approval prompt.
- Smoke test passes on the promoted prod deploy.

#### Definition of Done

- `.github/workflows/deploy.yml` has no `production` job.
- `promote.yml` refuses to promote a SHA that isn't live on staging.
- Production deploy requires GitHub environment approval click (not just a
  UI dropdown choice).
- `npm run promote` dispatches the workflow from a developer's laptop.
- The deploy flow is documented end-to-end in `CLAUDE.md`.

#### Dependencies

- DNS CNAME and staging secrets live (pre-flight).
- `X-Opchain-Version` header on staging (already shipped Sprint 0 —
  `src/index.js:258`).

#### Estimated Effort

CLAUDE: 3 | USER: 1 (DNS check, `wrangler secret put --env staging` for 3–4
secrets, configure `production` GitHub environment reviewers)

---

### Sprint 7 Build Order

7a → 7b → 7c → 7d. Each lands as its own PR; CI gates added in 7b enforce the
new bars from then on. 7d is independent of 7a/b/c and can land in parallel
if the test harness sprints slip.

---

## Backlog (post-Sprint-7, not committed)

These come out of the audit but don't earn a roadmap sprint yet. Treat as
single-PR backlog items — pick up opportunistically.

| # | Item | Trigger |
|---|---|---|
| B1 | Per-page OG images (`/og/skills.png`, `/og/in-action.png`, …) using `@vercel/og` or a static set | When social sharing data shows the global image hurting CTR |
| B2 | Dependabot PR-landing audit — confirm weekly bumps actually merge | Quarterly hygiene; first one this month |
| B3 | Worker handler TS migration (`src/opchain-try.js` → `.ts`) | Bundle with the next substantive handler change (e.g. multi-model support, rate-limit algorithm swap) |
| B4 | Feature-flag `/in-action` behind `?preview=1` until content review | If the page sees more iteration than expected |
| B5 | Visual-regression snapshots for the styleguide page | When a dedicated screenshot service (Percy / Chromatic) is justified |

These are small enough that the next person picking them up doesn't need a
sprint contract; the relevant section of `02-sprint-plan.md` or this file
is enough briefing.

---

## What this revised roadmap explicitly defers

Same as `02-sprint-plan.md` — no new product features. Specifically:

- New skills (the catalog stays at 10).
- Light-mode polish beyond what tokens already permit.
- D1 / R2 migration; KV is fine for the lead store at current volume.
- Pricing / commercial surface.
- i18n.
- Pan-zoom / interactive mermaid; static SVG is sufficient.

---

## Gate protocol

Per `skills/app-architect/SKILL.md` L269–L270, this roadmap requires
approval before Sub-sprint 7a starts:

- **Approve →** I update `.checkpoints/app-architect.checkpoint.json`
  status to `in_progress`, sprint `sprint-7`, and we begin contract
  negotiation for 7a.
- **Redirect →** point to any sub-sprint or threshold above and I rewrite
  the affected section.
- **Pause →** the document stays as a record; nothing builds.

---

## Total effort (estimate)

| Sub-sprint | CLAUDE hrs | USER hrs |
|---|---|---|
| 7a — Playwright e2e harness | 7 | 0 |
| 7b — Lighthouse + Axe gates | 5 | 0 |
| 7c — CSP nonce migration | 2 | 1 |
| 7d — Staging-first promotion | 3 | 1 |
| **Total** | **17 hrs** | **2 hrs** |

Calendar: 2–3 build sessions on the Generator → Evaluator loop, ~1 week
end-to-end if run consecutively.
